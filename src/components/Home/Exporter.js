import React from 'react';

function Exporter() {  
    return (
        <div className="exporter">
            {/* Add a button, click open a modal with form to add new export */}
            {/* Add exported data table here */}
        </div>
    );
}

export default Exporter;